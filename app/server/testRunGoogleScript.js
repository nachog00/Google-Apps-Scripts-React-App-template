/* 
This function is just a tester for the runGoogleScript helper function defined within the react app.
You can delete this file if you want.
*/

const testRunGoogleScript = (args) => {

    return `Hi from the GAS server!\n` + 
    `The runGoogleScript function is working properly.\n` +
    `You can check the console for the arguments you passed`

}